name: MobileDevAssignment
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  cached_network_image: ^3.3.1
  supabase_flutter: ^2.0.0
  provider: ^6.0.5
  shared_preferences: ^2.2.2
  flutter_map: ^8.3.0
  location: ^8.0.1
  permission_handler: ^12.0.1
  geocoding: ^3.0.0
  app_links: ^3.4.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
